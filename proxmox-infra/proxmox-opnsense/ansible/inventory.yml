---
all:
  hosts:
    opnsense:
      ansible_host: "10.10.10.1"
      ansible_connection: local  # ansibleguy.opnsense uses REST API — no SSH
  vars:
    ansible_python_interpreter: /usr/bin/python3
